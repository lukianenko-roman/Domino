<Window x:Class="Domino.WindowNotificationTwoSidesOK"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        ShowInTaskbar ="False"
        WindowStyle="ToolWindow"
        WindowStartupLocation="CenterOwner"
        
        Title="Which side put bone on?" SizeToContent="WidthAndHeight">
    <Grid>
        <StackPanel>
            <TextBlock Margin="10">You can put this bone to both sides.<LineBreak/><LineBreak/>Please use left mouse button to put on left side<LineBreak/>or rigth mouse button to put on right side.</TextBlock>
            <CheckBox x:Name="CheckBoxIsShowThisAgain" Margin="10 0 0 0">Do not show again</CheckBox>
            <Button x:Name="ButtonOK" Height="20" Margin="10" Click="Button_Click">Put according to click</Button>
            <Button x:Name="Cancel" Height="20" Margin="10" Click="Button_Click">Change choise</Button>
        </StackPanel>
    </Grid>
</Window>
